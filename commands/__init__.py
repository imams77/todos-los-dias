"""Commands package (wrappers) that run the original todo scripts but
operate on the repository 'data' directory. This keeps the original
implementations unchanged while providing a clean commands/ layout.
"""
